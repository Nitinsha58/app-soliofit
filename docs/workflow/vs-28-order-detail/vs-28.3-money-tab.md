# VS-28.3 — Money Tab (snapshot ↔ full plan editor)

**Program:** [VS-28 overview](./00-overview.md) · **Layer:** Frontend · **Status:** Pending

> **Frontend Design Gate:** read `07-ux-guidelines.md §0` and run §0.10 before editing this UI.

Make the Money tab the home of the payment plan: a clear bill/paid/outstanding header with the
paid items and the **VS-27.5 `PaymentSchedule`** whole-plan editor ("Edit bill & plan"). Wire the
Overview read-only snapshot's "View plan ›" to land here.

## Scope (in)

- Money tab: bill/paid/outstanding summary + paid items (locked) + the unpaid plan editor (reuse
  `PaymentSchedule` from VS-27.5; strict invariant + atomic `PUT /billing/` unchanged).
- Overview `OverviewPaymentCard` "View plan ›" → Money tab; optional installments preview.

## Scope (out)

- No billing/backend changes — strict invariant and endpoints are fixed by VS-27.

## Done when

Money presents the full, atomic plan editor; Overview's snapshot links cleanly into it; paid rows
stay locked.
