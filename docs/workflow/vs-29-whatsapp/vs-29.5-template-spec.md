# VS-29.5 — Finalized WhatsApp Template Spec (vault §10)

**Program:** [VS-29 overview](./00-overview.md) · **Layer:** Vault / product · **Status:** Pending
**ADR:** [ADR-0010](../../adr/ADR-0010-whatsapp-click-to-chat-messaging.md)

Land the finalized WhatsApp template set as the product source of truth and the seed defaults
for the future Template Management module. **Spec first, then execution** (29.6–29.8 reference
this doc). No application code in this slice.

## Scope (in)

1. **New vault doc `10-whatsapp-templates.md`** in the product vault
   (`/Users/nitin/MemoryGraph/Soliofit/Soliofit/`), capturing the finalized set verbatim with the
   agreed corrections baked in:
   - **Three template types:** `order_status`, `payment_context` (insertable fragment, not a
     standalone send), `payment_reminder` (separate collection track — future).
   - **`order_status` templates:** Booked, Started, Ready, Delivered (warm "ji" tone, multiline
     `Hi {customer} ji,` … `— {boutique_name}` structure).
   - **Partial Delivery:** **no messaging for now** — explicitly documented as skipped/future,
     pending a fix to the Partial Delivery model. Not given a template here.
   - **Delay:** documented as **future** (separate `action_key`, not an order status).
   - **`payment_context` state table** (Booked / Ready / Delivered only).
   - **`{item}` resolves to "order"** for now (interim until a structured order-item field exists).
   - **`{pickup_window}`** derived from boutique working hours (29.6/29.8).
2. **Refined future schema** recorded in the doc: `key`, `type`, `trigger`, **`action_key`**,
   `status`, `payment_condition`, `language`, `body`, `placeholders[]`, `is_default`, `active` —
   where `status` is for status-bound templates and `action_key` covers `delay_notice`,
   `payment_reminder_gentle/_followup/_consolidated`. `payment_context` stays an insertable
   fragment resolved at send-time by `payment_condition`.
3. **Placeholder reference** + the "avoid" guidance (robotic / harsh / long-policy).
4. Update `00-index.md` doc table to list doc 10. Commit in the vault.

## Scope (out)

- No frontend/backend code (that is 29.6–29.8).
- Partial Delivery messaging, Delay, payment reminders, `action_key` logging, structured item
  capture — all **documented as future**, not built.

## Done when

`10-whatsapp-templates.md` exists in the vault (indexed in `00-index.md`, committed), records the
finalized set + corrections + refined future schema, and is the single reference 29.6–29.8 build
against.
